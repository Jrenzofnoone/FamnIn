<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FarmIn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>
    <style name="Theme.FarmIn" parent="Base.Theme.FarmIn" />
    <style name="TextInputLayout" >
        <item name="boxStrokeColor">#ffffff</item>
        <item name="cursorColor">#ffffff</item>
        <item name="android:textColorHint">#ffffff</item>
        <item name="hintTextColor">#ffffff</item>
    </style>
</resources>