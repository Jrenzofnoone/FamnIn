<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FarmIn" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FarmIn" parent="Base.Theme.FarmIn" />
    <style name="TextInputLayout" >
        <item name="boxStrokeColor">#ffffff</item>
        <item name="cursorColor">#ffffff</item>
        <item name="android:textColorHint">#ffffff</item>
        <item name="hintTextColor">#ffffff</item>
    </style>
    <style name="forgotTextInputLayout" >
        <item name="boxStrokeColor">#1C36C4</item>
        <item name="cursorColor">@color/main</item>
        <item name="android:textColorHint">@color/main</item>
        <item name="hintTextColor">@color/main</item>
    </style>
</resources>